{
  "json_schema_major_version": "1",
  "json_schema_minor_version": "2",
  "json_content_version": "1",
  "function_name": "Pivot",
  "function_version": "1.8",
  "function_type": "non-driver",
  "function_r_name": "aa.pivot",
  "short_description": "This function takes a table as input, and converts rows to columns.",
  "long_description": "This function takes a table as input and converts rows to columns based on a column which has certain key values in it",
  "input_tables": [
    {
      "isOrdered": false,
      "useInR": true,
      "name": "input",
      "rName": "data",
      "isRequired": true,
      "datatype": "TABLE_ALIAS",
      "rOrderNum": 1,
      "allowsLists": false,
      "partitionByOne": false,
      "rDescription": "The table containing the data to be pivoted.",
      "requiredInputKind": [
        "PartitionByKey"
      ],
      "alternateNames": [],
      "description": "The table containing the data to be pivoted."
    }
  ],
  "argument_clauses": [
    {
      "name": "PartitionColumns",
      "allowsLists": true,
      "useInR": true,
      "rName": "partition.columns",
      "allowedTypes": [],
      "datatype": "COLUMNS",
      "targetTable": [
        "input"
      ],
      "rOrderNum": 2,
      "checkDuplicate": true,
      "isRequired": true,
      "rDescription": "Specifies the same columns as the partition.columns clause (in any order).",
      "alternateNames": [
        "Partitions"
      ],
      "allowedTypeGroups": [
        "ALL"
      ],
      "allowPadding": true,
      "matchLengthOfArgument": "",
      "description": "Specifies the same columns as the PARTITION BY clause (in any order)."
    },
    {
      "name": "TargetColumns",
      "allowsLists": true,
      "useInR": true,
      "rName": "target.columns",
      "allowedTypes": [],
      "datatype": "COLUMNS",
      "targetTable": [
        "input"
      ],
      "rOrderNum": 3,
      "checkDuplicate": true,
      "isRequired": true,
      "rDescription": " Specifies the names of the input columns that contain the values to pivot.",
      "alternateNames": [
        "Metrics"
      ],
      "allowedTypeGroups": [
        "ALL"
      ],
      "allowPadding": true,
      "matchLengthOfArgument": "",
      "description": " Specifies the names of the input columns that contain the values to pivot."
    },
    {
      "name": "PivotColumn",
      "allowsLists": false,
      "useInR": true,
      "rName": "pivot.column",
      "allowedTypes": [],
      "datatype": "COLUMNS",
      "targetTable": [
        "input"
      ],
      "rOrderNum": 4,
      "checkDuplicate": true,
      "isRequired": false,
      "rDescription": " Specifies the name of the column that contains the pivot keys. If the pivot column contains numeric values, then the function casts them to VARCHAR. If you omit the num.rows argument, then you must specify this argument. Note: If you specify the pivot.column argument, then you must order the input data; otherwise, the output table column content is nondeterministic.",
      "requiredLength": 1,
      "alternateNames": [],
      "allowedTypeGroups": [
        "ALL"
      ],
      "allowPadding": true,
      "matchLengthOfArgument": "",
      "description": "Specifies the name of the column that contains the pivot keys. If the pivot column contains numeric values, then the function casts them to VARCHAR. If you omit the NumberOfRows argument, then you must specify this argument. Note: If you specify the PivotColumn argument, then you must order the input data; otherwise, the output table column content is nondeterministic."
    },
    {
      "name": "NumberOfRows",
      "upperBoundType": "INCLUSIVE",
      "lowerBound": 1,
      "useInR": true,
      "rName": "num.rows",
      "datatype": "INTEGER",
      "allowNaN": false,
      "rOrderNum": 7,
      "allowsLists": false,
      "upperBound": 2147483647,
      "lowerBoundType": "INCLUSIVE",
      "isRequired": false,
      "rDescription": "Specifies the maximum number of rows in any partition. If a partition has fewer than number_of_rows rows, then the function adds NULL values; if a partition has more than number_of_rows rows, then the function omits the extra rows. If you omit this argument, then you must specify the pivot.column argument. Note: With this argument, the ORDER BY clause is optional. If omitted, the order of values can vary. The function adds NULL values at the end.",
      "alternateNames": [
        "Rows"
      ],
      "description": "Specifies the maximum number of rows in any partition. If a partition has fewer than number_of_rows rows, then the function adds NULL values; if a partition has more than number_of_rows rows, then the function omits the extra rows. If you omit this argument, then you must specify the PivotColumn argument. Note: With this argument, the ORDER BY clause is optional. If omitted, the order of values can vary. The function adds NULL values at the end."
    },
    {
      "name": "PivotKeys",
      "allowsLists": true,
      "isOutputColumn": false,
      "useInR": true,
      "datatype": "STRING",
      "rOrderNum": 5,
      "rName": "pivot.keys",
      "isRequired": false,
      "rDescription": "If you specify the pivot.column argument, then this argument specifies the names of the pivot keys. Do not use this argument without the pivot.column argument. If pivot.column contains a value that is not specified as a pivot.key, then the function ignores the row containing that value. pivot.keys is required when pivot.columns is used.",
      "alternateNames": [],
      "permittedValues": [],
      "allowPadding": true,
      "matchLengthOfArgument": "",
      "description": " If you specify the PivotColumn argument, then this argument specifies the names of the pivot keys. Do not use this argument without the PivotColumn argument. If pivot_column contains a value that is not specified as a pivot_key, then the function ignores the row containing that value. pivot_keys is required when pivot_columns is used."
    },
    {
      "name": "SchemaName",
      "allowsLists": false,
      "isOutputColumn": false,
      "useInR": true,
      "datatype": "STRING",
      "rOrderNum": 8,
      "rName": "schema.name",
      "isRequired": false,
      "rDescription": "Schema name of the input table if it is not in the public schema",
      "alternateNames": [],
      "permittedValues": [],
      "description": "Schema name of the input table if it is not in the public schema"
    },
    {
      "name": "NumericPivotKey",
      "allowsLists": false,
      "useInR": true,
      "datatype": "BOOLEAN",
      "defaultValue": false,
      "rOrderNum": 6,
      "rName": "numeric.pivotkey",
      "isRequired": false,
      "rDescription": "indicates whether the pivot key values are numeric values.",
      "alternateNames": [],
      "description": "indicates whether the pivot key values are numeric values."
    }
  ]
}